#logging.level.com.zaxxer.hikari.HikariConfig=DEBUG

spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=user
spring.datasource.password=pass
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

spring.h2.console.enabled=true
spring.jpa.show-sql=true

spring.jpa.hibernate.ddl-auto=create

# none
# nic si? nie dzieje

# validate
# sprawdzenie schematu DB z mapowaniem encji, ale nie wprowadza zmian.

# update
# aktualizuja schematu DB do kodu, ale istniej?ce dane nie s? usuwane.

# create
# stworzenie nowego schematu DB przy ka?dym uruchomieniu aplikacji, niszcz?c istniej?ce dane.

# create-drop
# stworzenie nowego schematu DB przy starcie aplikacji, a nast?pnie usuni?cie go przy zamykaniu.
